import React from 'react';
import Icon from 'react-native-vector-icons/Ionicons';

const AppIcon = ({name, size, color, style}) => {
  return <Icon name={name} size={size} color={color} style />;
};

export default AppIcon;
